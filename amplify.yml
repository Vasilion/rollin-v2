version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cp next.config.build.ts next.config.ts
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: out
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
